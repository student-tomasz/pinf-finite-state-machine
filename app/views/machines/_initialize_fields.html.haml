%ol
  %li
    = f.label :name
    = f.text_field :name
  - [:states, :alphabet].each do |attribute|
    %li
      = f.label attribute
      = f.text_area attribute, :value => @machine.send(attribute).join("\n")
